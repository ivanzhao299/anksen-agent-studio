{
  "schema_version": 1,
  "updated_at": "2026-06-23T00:00:00.000Z",
  "secret_values": "forbidden",
  "env_read": "forbidden",
  "keychain_read": "forbidden",
  "external_vault_read": "forbidden",
  "allowed_reference_types": [
    "vault_path",
    "env_ref",
    "keychain_ref",
    "external_vault_ref"
  ],
  "forbidden_fields": [
    "api_key",
    "secret",
    "token",
    "password",
    "private_key",
    "ssh_key"
  ],
  "runtime_health_secret_access": "reference_presence_only",
  "notes": "MVP stores and validates references only. It must never read real env values, keychain items, external vault secrets, API keys, or SSH private keys."
}
