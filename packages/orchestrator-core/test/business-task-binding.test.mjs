import test from "node:test";import assert from "node:assert/strict";import {createBusinessTaskBinding,isBusinessTaskBinding} from "../lib/business-task-binding.mjs";
const valid=()=>({scope:{organizationId:"org",workspaceId:"workspace",projectId:"project",applicationId:"finance-platform",domainId:"finance-management",userId:"finance-user"},businessObject:{systemId:"finance",objectType:"expense",objectId:"EXP-1",version:3,href:"/finance?record=EXP-1"},workflow:{definitionId:"expense-review",definitionVersion:"1",instanceId:"wf-1",stageId:"VALIDATE"},skill:{businessSkillId:"expense_compliance",skillId:"validation-testing",skillType:"validation_testing",requiredCapabilities:["finance.read"],riskLevel:"MEDIUM"},execution:{assignmentPolicy:"CAPABILITY",preferredRuntimeId:"controlled-stub"},writeback:{operation:"TRANSITION",expectedObjectVersion:3,eventType:"finance.expense.reviewed"}});
test("business binding preserves authoritative object, workflow, skill and writeback scope",()=>{const value=createBusinessTaskBinding(valid());assert.equal(value.businessObject.objectType,"expense");assert.equal(value.workflow.instanceId,"wf-1");assert.equal(value.writeback.expectedObjectVersion,3);assert.match(value.skill.idempotencyKey,/EXP-1/);assert.equal(isBusinessTaskBinding(value),true);});
test("business binding rejects application object mismatch and unpinned worker",()=>{const mismatch=valid();mismatch.businessObject.objectType="employee";assert.throws(()=>createBusinessTaskBinding(mismatch),error=>error.code==="BUSINESS_OBJECT_TYPE_DENIED");const pinned=valid();pinned.execution.assignmentPolicy="PINNED";assert.throws(()=>createBusinessTaskBinding(pinned),error=>error.code==="ASSIGNMENT_POLICY_INVALID");});
