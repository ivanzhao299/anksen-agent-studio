import { randomUUID } from "node:crypto";
import { existsSync } from "node:fs";
import { mkdir, readFile, readdir, rm, writeFile } from "node:fs/promises";
import { resolve, join } from "node:path";
import { spawnSync } from "node:child_process";
import { validateGovernedCodexConfig } from "./governed-codex-config.mjs";
import { approvalScopeDigest, captureGitWorkspace, deliveryReport } from "./autonomous-development-policy.mjs";
import { acceptanceEvidence, normalizeQueuePolicy, normalizeResourceBudget, operationalSnapshot, orderQueue, redactEvidence, releaseAssistance, sha256 } from "./autonomous-development-operations.mjs";
import { GitWorkspaceIsolationManager } from "../../project-connector/lib/git-workspace-isolation.mjs";

const terminal = new Set(["SUCCEEDED", "FAILED", "CANCELLED", "COMMITTED"]);
const safeId = value => String(value ?? "").replace(/[^a-z0-9._-]+/gi, "-").replace(/^-+|-+$/g, "").slice(0, 64);
const boundedInteger = (value, fallback, minimum, maximum, code) => {
  const parsed = Number(value ?? fallback);
  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) throw Object.assign(new Error(code), { code });
  return parsed;
};

async function suggestedAcceptanceCommands(projectRoot) {
  const commands = ["git diff --check"];
  try {
    const pkg = JSON.parse(await readFile(resolve(projectRoot, "package.json"), "utf8"));
    if (pkg.scripts?.typecheck) commands.push("pnpm typecheck");
    if (pkg.scripts?.test) commands.push("pnpm test");
    else if (pkg.scripts?.build) commands.push("pnpm build");
  } catch {}
  return commands;
}

export class AutonomousDevelopmentJobs {
  constructor({ repoRoot, storeDir = resolve(repoRoot, "runtime/autonomous-development"), clock = () => new Date(), processAlive = (pid) => { try { process.kill(pid, 0); return true; } catch { return false; } } } = {}) {
    this.repoRoot = resolve(repoRoot);
    this.storeDir = storeDir;
    this.clock = clock;
    this.processAlive = processAlive;
    this.workspaceIsolation = new GitWorkspaceIsolationManager({ stateDir: resolve(storeDir, "workspace-ownership"), worktreeRoot: resolve(storeDir, "worktrees"), clock });
  }
  jobPath(id) { return join(this.storeDir, "jobs", `${safeId(id)}.json`); }
  artifactDir(id) { return join(this.storeDir, "artifacts", safeId(id)); }
  heartbeatPath() { return join(this.storeDir, "worker-heartbeat.json"); }
  async save(job) { await mkdir(resolve(this.jobPath(job.id), ".."), { recursive: true }); job.updatedAt = this.clock().toISOString(); await writeFile(this.jobPath(job.id), `${JSON.stringify(job, null, 2)}\n`, "utf8"); return job; }
  async get(id) { return existsSync(this.jobPath(id)) ? JSON.parse(await readFile(this.jobPath(id), "utf8")) : null; }
  async list() { const dir = resolve(this.storeDir, "jobs"); if (!existsSync(dir)) return []; const files = (await readdir(dir)).filter(name => name.endsWith(".json")); const rows = await Promise.all(files.map(name => readFile(join(dir, name), "utf8").then(JSON.parse))); return rows.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))); }
  async event(job, type, payload = {}) { job.events.push({ sequence: job.events.length + 1, type, at: this.clock().toISOString(), ...payload }); return this.save(job); }
  async artifact(job, type, content, metadata = {}) { const id = `${String(job.artifacts.length + 1).padStart(3,"0")}-${safeId(type)}`; await mkdir(this.artifactDir(job.id), { recursive: true }); const path = join(this.artifactDir(job.id), `${id}.txt`); const evidence=redactEvidence(content); await writeFile(path, evidence.text, "utf8"); const record = { id, type, path, sha256:sha256(evidence.text),redacted:evidence.redacted,createdAt: this.clock().toISOString(), ...metadata }; job.artifacts.push(record); await this.save(job); return record; }
  async readArtifact(jobId, artifactId) { const job=await this.get(jobId); const artifact=job?.artifacts.find(item=>item.id===artifactId),root=resolve(this.artifactDir(jobId)); if(!artifact||!(resolve(artifact.path)===root||resolve(artifact.path).startsWith(`${root}/`))) return null; const content=await readFile(artifact.path,"utf8");if(artifact.sha256&&sha256(content)!==artifact.sha256)throw Object.assign(new Error("ARTIFACT_INTEGRITY_FAILED"),{code:"ARTIFACT_INTEGRITY_FAILED"});return { artifact, content }; }
  async create(input, actor = {}) {
    const id = `dev-${Date.now()}-${randomUUID().slice(0,8)}`;
    const goal = String(input.goal ?? "").trim();
    const acceptanceCriteria = Array.isArray(input.acceptanceCriteria) ? input.acceptanceCriteria.map(String).map(v=>v.trim()).filter(Boolean) : String(input.acceptanceCriteria ?? "").split("\n").map(v=>v.trim()).filter(Boolean);
    const allowedPaths = Array.isArray(input.allowedPaths) ? input.allowedPaths : String(input.allowedPaths ?? "").split("\n").map(v=>v.trim()).filter(Boolean);
    const maxRepairAttempts = boundedInteger(input.maxRepairAttempts, 1, 0, 2, "REPAIR_BUDGET_INVALID");
    const inferredAcceptanceCommands = await suggestedAcceptanceCommands(input.projectRoot);
    const validation = validateGovernedCodexConfig({ runKey: id, projectId: safeId(input.projectId || "development-project"), projectRoot: input.projectRoot, goal, instruction: goal, allowedPaths, targetPaths: allowedPaths, blockedPaths: [".env", ".env.*", ".git", "node_modules", ".ssh", "deploy", "infra", "terraform"], acceptanceCommands: input.acceptanceCommands?.length ? input.acceptanceCommands : inferredAcceptanceCommands, maxRuntimeSeconds: Number(input.maxRuntimeSeconds ?? 1800), credentialReferenceId: "codex-local-session-ref", policyVersion: `${id}-v1` });
    const evidence=acceptanceEvidence(acceptanceCriteria,validation.acceptanceCommands,input.acceptanceEvidence??[]),queuePolicy=normalizeQueuePolicy(input),resourceBudget=normalizeResourceBudget(input.resourceBudget,validation.maxRuntimeSeconds);
    const questions = [...(goal.length < 20 ? ["请补充更具体的预期结果和业务边界。"] : []), ...(acceptanceCriteria.length === 0 ? ["请至少提供一项可验证的验收标准。"] : []), ...(acceptanceCriteria.length&&evidence.status!=="READY"?["请为每项验收标准映射允许的命令、测试或明确的独立审查证据。"]:[])];
    const workspaceIsolationEnabled = input.workspaceIsolation === true;
    const legacyBaseline = workspaceIsolationEnabled ? null : captureGitWorkspace(validation.projectRoot);
    const sourceWorkspace = workspaceIsolationEnabled ? await this.workspaceIsolation.inspect({ projectId: validation.projectId, projectRoot: validation.projectRoot }) : null;
    const preflightBlocked = !workspaceIsolationEnabled && legacyBaseline.paths.length > 0;
    const job = { schemaVersion: 4, id, status: questions.length ? "NEEDS_CLARIFICATION" : preflightBlocked ? "PREFLIGHT_BLOCKED" : "PENDING_APPROVAL", goal, projectId: validation.projectId, projectRoot: validation.projectRoot, sourceProjectRoot: validation.projectRoot, defaultBranch: String(input.defaultBranch ?? "main"), workspaceIsolationEnabled, isolatedWorkspace: null, allowedPaths: validation.allowedPaths, blockedPaths: validation.blockedPaths, acceptanceCommands: validation.acceptanceCommands, acceptanceCommandSource: input.acceptanceCommands?.length ? "USER" : "INFERRED", acceptanceCriteria,acceptanceEvidence:evidence,queuePolicy,resourceBudget,maxRuntimeSeconds: validation.maxRuntimeSeconds, maxRepairAttempts, repairAttemptsUsed: 0, attemptSummaries: [], credentialReferenceId: validation.credentialReferenceId, clarification: { questions, answer: null }, preflight: { status: preflightBlocked ? "BLOCKED" : "PASS", baselineDigest: sourceWorkspace?.digest ?? legacyBaseline.digest, existingChangedPaths: sourceWorkspace ? sourceWorkspace.changes.map(item=>item.path) : legacyBaseline.paths, existingChangeOwnership: sourceWorkspace?.changes ?? [] }, requestedBy: actor.userId ?? "unknown", approvedBy: null, approvedAt: null, approvalScope: null, approvalScopeDigest: null, workerId: null, workerPid: null, stage: "PREFLIGHT", agentInstances: [], artifacts: [], events: [], tokenUsage: { inputTokens: 0, cachedInputTokens: 0, outputTokens: 0, reasoningOutputTokens: 0 },runtimeUsageSeconds:0, changedPaths: [], validation: null, validationHistory: [], review: null, delivery: null,releaseAssistance:null, commit: null, error: null, recovery: null, createdAt: this.clock().toISOString(), updatedAt: this.clock().toISOString() };
    await this.event(job, "JOB_CREATED", { status: job.status });
    return job;
  }
  async clarify(id, answer, actor = {}) { const job=await this.get(id); if(!job) return null; if(job.status!=="NEEDS_CLARIFICATION") throw new Error("JOB_NOT_WAITING_FOR_CLARIFICATION"); if(!String(answer??"").trim()) throw new Error("CLARIFICATION_REQUIRED"); job.clarification.answer=String(answer).trim(); job.clarification.answeredBy=actor.userId??"unknown"; if(job.acceptanceEvidence?.status!=="READY")return this.event(job,"CLARIFICATION_RECEIVED",{actorId:actor.userId??"unknown",evidenceStillBlocked:true});job.status="PENDING_APPROVAL"; return this.event(job,"CLARIFICATION_RECEIVED",{actorId:actor.userId??"unknown"}); }
  async approve(id, actor = {}) { const job=await this.get(id); if(!job) return null; if(!["PENDING_APPROVAL","PAUSED"].includes(job.status)) throw new Error("JOB_NOT_APPROVABLE");if(job.acceptanceEvidence?.status!=="READY")throw Object.assign(new Error("ACCEPTANCE_EVIDENCE_REQUIRED"),{code:"ACCEPTANCE_EVIDENCE_REQUIRED"});let isolated=null;if(job.workspaceIsolationEnabled){const source=await this.workspaceIsolation.inspect({projectId:job.projectId,projectRoot:job.sourceProjectRoot??job.projectRoot});if(source.digest!==job.preflight.baselineDigest)throw Object.assign(new Error("PROJECT_BASELINE_CHANGED"),{code:"PROJECT_BASELINE_CHANGED"});isolated=await this.workspaceIsolation.createTaskWorkspace({projectId:job.projectId,taskId:job.id,projectRoot:source.projectRoot,defaultBranch:job.defaultBranch,actorId:actor.userId??"unknown",allowedPaths:job.allowedPaths});job.isolatedWorkspace=isolated.workspace;job.projectRoot=isolated.workspace.worktreePath;}else{const baseline=captureGitWorkspace(job.projectRoot);if(baseline.paths.length||baseline.digest!==job.preflight.baselineDigest)throw Object.assign(new Error("PROJECT_BASELINE_CHANGED"),{code:"PROJECT_BASELINE_CHANGED"});} job.status="QUEUED"; job.stage="QUEUED";job.queuedAt=this.clock().toISOString(); job.approvedBy=actor.userId??"unknown"; job.approvedAt=this.clock().toISOString(); job.approvalExpiresAt=new Date(this.clock().getTime()+4*60*60*1000).toISOString(); job.approvalScope={projectId:job.projectId,projectRoot:job.projectRoot,sourceProjectRoot:job.sourceProjectRoot,sourceDigest:isolated?.workspace.sourceDigest??null,remoteHead:isolated?.workspace.remoteHead??null,branch:isolated?.workspace.branch??null,allowedPaths:[...job.allowedPaths],blockedPaths:[...job.blockedPaths],acceptanceCommands:[...job.acceptanceCommands],acceptanceEvidence:job.acceptanceEvidence,queuePolicy:job.queuePolicy,resourceBudget:job.resourceBudget,maxRuntimeSeconds:job.maxRuntimeSeconds,maxRepairAttempts:job.maxRepairAttempts,commit:false,push:false,merge:false,deploy:false}; job.approvalScopeDigest=approvalScopeDigest(job.approvalScope); return this.event(job,"JOB_APPROVED",{actorId:job.approvedBy,approvalScopeDigest:job.approvalScopeDigest,approvalExpiresAt:job.approvalExpiresAt,branch:isolated?.workspace.branch??null,worktreePath:isolated?.workspace.worktreePath??null,remoteHead:isolated?.workspace.remoteHead??null}); }
  async pause(id, actor={}) { const job=await this.get(id); if(!job||terminal.has(job.status)) return job; job.status="PAUSED"; return this.event(job,"JOB_PAUSED",{actorId:actor.userId??"unknown"}); }
  async cancel(id, actor={}) { const job=await this.get(id); if(!job||terminal.has(job.status)) return job; job.status="CANCELLED"; return this.event(job,"JOB_CANCELLED",{actorId:actor.userId??"unknown"}); }
  async claim(worker) { const rows=await this.list();for(const row of orderQueue(rows,this.clock())){row.status="RUNNING"; row.stage=row.stage==="QUEUED"?"PLANNING":row.stage; row.workerId=worker.id; row.workerPid=worker.pid; row.startedAt=row.startedAt??this.clock().toISOString(); await this.event(row,"WORKER_CLAIMED",{workerId:worker.id,pid:worker.pid,stage:row.stage,priority:row.queuePolicy?.priority}); return row;} return null; }
  async heartbeat(worker, currentJobId = null) { const value={workerId:worker.id,pid:worker.pid,status:currentJobId?"BUSY":"IDLE",currentJobId,lastHeartbeatAt:this.clock().toISOString(),runtimeType:"CODEX",realProcess:true}; await mkdir(this.storeDir,{recursive:true}); await writeFile(this.heartbeatPath(),`${JSON.stringify(value,null,2)}\n`,`utf8`); return value; }
  async workerStatus() { if(!existsSync(this.heartbeatPath())) return {status:"OFFLINE",reason:"NO_HEARTBEAT"}; const value=JSON.parse(await readFile(this.heartbeatPath(),"utf8")); const ageMs=this.clock().getTime()-new Date(value.lastHeartbeatAt).getTime(); return {...value,status:ageMs>15000?"OFFLINE":value.status,heartbeatAgeMs:Math.max(0,ageMs)}; }
  async reconcileOrphanedJobs() { const recovered=[]; for(const job of await this.list()){ if(job.status!=="RUNNING"||this.processAlive(Number(job.workerPid)))continue; const sideEffectsPossible=["IMPLEMENTING","REPAIRING"].includes(job.stage); job.recovery={decidedAt:this.clock().toISOString(),previousWorkerPid:job.workerPid,previousStage:job.stage,sideEffectsPossible,decision:sideEffectsPossible?"HUMAN_REVIEW_REQUIRED":"SAFE_REQUEUE"}; if(sideEffectsPossible){job.status="RECOVERY_REQUIRED";}else{job.status="QUEUED";job.stage="QUEUED";} job.workerId=null;job.workerPid=null;await this.event(job,"ORPHAN_RECONCILED",job.recovery);recovered.push(job);} return recovered; }
  async recordDelivery(job) {if(job.startedAt)job.runtimeUsageSeconds=Math.round((this.clock().getTime()-new Date(job.startedAt).getTime())/1000); job.delivery=deliveryReport(job);job.releaseAssistance=releaseAssistance(job); await this.artifact(job,"delivery-report",JSON.stringify(job.delivery,null,2));await this.artifact(job,"release-assistance",JSON.stringify(job.releaseAssistance,null,2)); return job.delivery; }
  async operations() {const jobs=await this.list(),worker=await this.workerStatus();return operationalSnapshot(jobs,worker,this.clock());}
  async auditExport() {const jobs=await this.list();return{schemaVersion:1,generatedAt:this.clock().toISOString(),jobs:jobs.map(job=>({id:job.id,projectId:job.projectId,status:job.status,approvalScopeDigest:job.approvalScopeDigest,events:job.events,artifacts:job.artifacts.map(({id,type,sha256,redacted,createdAt})=>({id,type,sha256,redacted,createdAt}))}))};}
  async cleanup({retentionDays=30,dryRun=true}={}) {const cutoff=this.clock().getTime()-retentionDays*86400000,removed=[];for(const job of await this.list()){if(!terminal.has(job.status)||new Date(job.finishedAt??job.updatedAt).getTime()>=cutoff)continue;removed.push(job.id);if(!dryRun){await rm(this.artifactDir(job.id),{recursive:true,force:true});await rm(this.jobPath(job.id),{force:true});}}return{dryRun,retentionDays,removed};}
  async approveCommit(id, actor={}) { const job=await this.get(id); if(!job) return null; if(job.status!=="AWAITING_DIFF_APPROVAL") throw new Error("JOB_NOT_READY_FOR_COMMIT"); const status=spawnSync("git",["-C",job.projectRoot,"status","--short","--untracked-files=all"],{encoding:"utf8"}); if(status.status!==0) throw new Error("GIT_STATUS_FAILED"); const paths=status.stdout.split("\n").filter(Boolean).map(line=>line.slice(3).split(" -> ").at(-1)); if(paths.some(path=>!job.allowedPaths.some(allowed=>path===allowed||path.startsWith(`${allowed.replace(/\/$/,"")}/`)))) throw new Error("COMMIT_PATH_DENIED"); const add=spawnSync("git",["-C",job.projectRoot,"add","--",...paths],{encoding:"utf8"}); if(add.status!==0) throw new Error("GIT_ADD_FAILED"); const commit=spawnSync("git",["-C",job.projectRoot,"commit","-m",`feat(autonomous): ${job.goal.slice(0,60)}`],{encoding:"utf8"}); if(commit.status!==0) throw new Error(commit.stderr||"GIT_COMMIT_FAILED"); const rev=spawnSync("git",["-C",job.projectRoot,"rev-parse","HEAD"],{encoding:"utf8"}); job.status="COMMITTED"; job.commit={hash:rev.stdout.trim(),approvedBy:actor.userId??"unknown",committedAt:this.clock().toISOString(),pushed:false}; return this.event(job,"COMMIT_APPROVED",job.commit); }
}
