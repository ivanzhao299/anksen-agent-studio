import { PersistentDomainWorkflowService } from "./persistent-domain-workflow.mjs";

const recordWritebackErrors=new Set(["BUSINESS_RECORD_VERSION_CONFLICT","BUSINESS_RECORD_TRANSITION_DENIED","BUSINESS_RECORD_NOT_FOUND"]);

export class PersistentBusinessWorkExecutor {
  constructor({store,registry,acquirePool,maxCycles=100}={}){if(!store?.getWorkItemForRunner||!store?.completeWorkflow)throw Object.assign(new Error("BUSINESS_WORK_STORE_REQUIRED"),{code:"BUSINESS_WORK_STORE_REQUIRED"});if(!registry)throw Object.assign(new Error("REGISTRY_REQUIRED"),{code:"REGISTRY_REQUIRED"});if(typeof acquirePool!=="function")throw Object.assign(new Error("BUSINESS_WORK_POOL_REQUIRED"),{code:"BUSINESS_WORK_POOL_REQUIRED"});this.store=store;this.registry=registry;this.acquirePool=acquirePool;this.maxCycles=Math.max(1,Math.min(Number(maxCycles)||100,1000));}
  async execute(item){let pool=null,ownsPool=false;try{({pool,ownsPool=false}=await this.acquirePool());const session=(await pool.query("SELECT session_key,status FROM ad_night_shift_session WHERE id=$1",[item.sessionId])).rows[0];if(!session)throw Object.assign(new Error("BUSINESS_WORK_SESSION_NOT_FOUND"),{code:"BUSINESS_WORK_SESSION_NOT_FOUND"});const service=new PersistentDomainWorkflowService(pool,{registry:this.registry});let status=session.status;for(let count=0;count<this.maxCycles&&status==="RUNNING";count+=1){const result=await service.runOnce(session.session_key);status=result.status??result.sessionStatus??result.report?.sessionStatus??"RUNNING";}if(status==="RUNNING")throw Object.assign(new Error("BUSINESS_WORK_RUNNER_LIMIT"),{code:"BUSINESS_WORK_RUNNER_LIMIT"});const report=await service.night.loadReport(item.sessionId),fresh=await this.store.getWorkItemForRunner(item.id);if(!fresh||fresh.status!=="RUNNING")return{status:"SKIPPED",reason:"WORK_ITEM_NOT_RUNNING"};const actor={organizationId:fresh.organizationId,workspaceId:fresh.workspaceId,userId:"resident-business-runner"},record=await this.store.getRecord(fresh.applicationId,fresh.businessObject.objectId,actor),succeeded=report?.sessionStatus==="SUCCEEDED",reviewStatus=record?.schema.agentReviewStatus,businessStatus=succeeded&&record?.availableTransitions.includes(reviewStatus)?reviewStatus:null,workStatus=succeeded?(businessStatus??"COMPLETED"):"BLOCKED";try{return await this.store.completeWorkflow(fresh.id,{goalId:fresh.kernelGoalId,sessionId:fresh.sessionId,report,workStatus,businessStatus,expectedObjectVersion:record?.version??null,expectedWorkVersion:fresh.version,actorId:actor.userId});}catch(error){if(!recordWritebackErrors.has(error.code))throw error;const latest=await this.store.getWorkItemForRunner(fresh.id);if(!latest||latest.status!=="RUNNING")return{status:"SKIPPED",reason:error.code};return this.store.completeWorkflow(latest.id,{goalId:latest.kernelGoalId,sessionId:latest.sessionId,report,workStatus:"BLOCKED",businessStatus:null,expectedWorkVersion:latest.version,actorId:actor.userId});}}
    finally{if(pool&&ownsPool)await pool.end();}}
}
